#include <iostream>
#include <cmath>
using namespace std;

int main()
{
setlocale(LC_ALL, "RUSSIAN");
cout << "Введите массив a[i]: " << endl;
cout << "Чтобы остановить ввод массива ВВЕДИТЕ 0:" << endl;
int j, i, n;
double s = 0;
int arr[100000];
n = 0;
for (i = 0;; ++i) {
cin >> arr[i];
n++;
if (arr[i] == 0) {
break;
}
}
cout << "Среднее аримфетическое массива a[i]: " << " ";
for (i = 0; i < n - 1; ++i) {
s = s + arr[i];
}
cout << s / i << endl;
return 0;
}
